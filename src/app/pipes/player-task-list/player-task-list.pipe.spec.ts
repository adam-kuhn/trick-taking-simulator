import { PlayerTaskListPipe } from './player-task-list.pipe';

describe('PlayerTaskListPipe', () => {
  it('create an instance', () => {
    const pipe = new PlayerTaskListPipe();
    expect(pipe).toBeTruthy();
  });
});
